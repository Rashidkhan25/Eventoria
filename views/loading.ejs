<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Loading Eventoria</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <style>
      body {
        background: black;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        overflow: hidden;
      }
      .loader-container {
        position: relative;
        width: 300px;
        height: 250px;
        z-index: 10;
      }
      .loader-title {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        font-size: 4rem;
        color: white;
        opacity: 0;
        z-index: 20;
        display: flex;
        gap: 3px;
      }
      .loader-title span {
        opacity: 0;
        transform: translateY(20px);
        display: inline-block;
        font-family: "zentry";
        font-weight: 800;
      }
      .loader-image {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        opacity: 0;
        transform: scale(0.8);
      }
    </style>
  </head>
  <body>
    <div class="loader-container">
      <h1 id="loaderTitle" class="loader-title font-zentry">EVENTORIA</h1>
      <% for(let i=1; i<=4; i++) { %>
      <img
        src="/images/loading<%= i %>.jpeg"
        class="loader-image"
        id="img<%= i %>"
      />
      <% } %>
      <video id="loaderVideo" muted playsinline>
        <source src="/videos/hero.mp4" type="video/mp4" />
      </video>
    </div>

    <div
      id="overlay"
      style="
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0;
      "
    ></div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <script src="/js/loading.js"></script>
  </body>
</html>
